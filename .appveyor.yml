build: off

branches:
  only:
    - master

stack: go 1.12

environment:
  matrix:
    - CC: gcc
      CHOCO_PKG: mingw
    - CC: clang
      CHOCO_PKG: llvm

install:
  - choco install make $CHOCO_PKG
  - refreshenv

build_script:
  - make all

test_script:
  - make test
